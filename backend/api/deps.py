# ── Shared FastAPI dependencies ─────────────────────────────────────
# Add reusable dependencies here as the app grows.
# Examples:
#   - get_current_user : verify JWT and return the authenticated user
#   - get_db           : yield a database session
#   - rate_limiter     : enforce per-user request limits
